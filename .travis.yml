language: python
python:
  - 3.6.6
services:
  - docker
install:
  - pip install -r capsulefyweb/requirements.txt
script:
  - "./manage.py test"
after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push raffrearaus/capsutesting